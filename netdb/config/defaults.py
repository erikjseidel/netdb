# Mongod host and port
MONGO_URL="mongodb://10.40.100.5:27017/"
# Mongod DB name
DB_NAME = 'netdb'
# This requires a mongodb relica set
TRANSACTIONS = True
# Used for instances on proxy minions and only need reads.
READ_ONLY = False
